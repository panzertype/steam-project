import { ExcludeItemsPipe } from './exclude-items.pipe';

describe('ExcludeItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
