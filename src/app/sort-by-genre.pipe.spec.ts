import { SortByGenrePipe } from './sort-by-genre.pipe';

describe('SortByGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
